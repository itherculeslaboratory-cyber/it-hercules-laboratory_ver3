---
id: HANDOFF-c5-c6-2026-07-11
title: C5→C6 外出ラン引き継ぎ正本(新セッション kickoff 用)
date: "2026-07-11"
status: active
---

# HANDOFF: C5(第1波A帯消化) → C6(cutover 直前まで)

> 新セッションはまず README → AGENTS.md → `docs/planning/status.md` → 本書の順で読むこと。
> 計画正本: `ihl-ver2/docs/planning/ver3/b3/ver3-開発計画-v1.md` §3.1 C5/C6・§4.3(C5 開始時の見積改訂義務)・§5.3(並行運用)・§7(K4/知の広場)。
> 自律ラン既定契約(グローバル CLAUDE.md)適用。批評家ゲート必須・誇張ゼロ・実測主義。

## 0. 現在地(2026-07-11 朝・全て push 済み・main = efa9364)

- **C0〜C4 完了**。テスト: TS 225(api 1 + contract 209 + web 15)+ pytest 4 passed/1 skipped・lint 9 GATE。裁定第11回まで記録済み(要件 725 件・srs v1.7)。
- 認証(署名付きステートレスセッション+マジックリンク+dev_token)・観測コア(V3-OBS-22 5機能+実ブラウザE2E)・ScreenDef Renderer(Next.js15+WCAG state7種)・類似検索梯子・CL-07 thumbnail(jSquash JPEG)・collector ingest(Ed25519+SwitchBot)・ruri parity(cosine=1.0)・台帳/カルマ二層・GMO sunabar 照合・market 骨格まで実装済み。
- **Resend 完全開通**(ドメイン verified・auth@it-hercules.uk 実送信確認済み)。
- 残人間ゲート: GMO 本番契約/live 昇格(+C4(ii)残余1点の確定)・collector 実鍵・LICENSE・公開・cutover 実施/VPS 解約・知の広場 HG-KN-01〜08。

## 1. ユーザー承認済み事項(2026-07-11・本セッション AskUserQuestion 逐語確認)

1. **staging デプロイ可**: wrangler で dev 環境(workers.dev / dev サブドメイン)へデプロイ+dev worker への secret 投入可(実行直前一言報告)。**本番ドメイン/route は一切不可**。
2. **本番 read-only 可**: ver3-live(api.it-hercules.uk)への GET 専用呼び出し可(C6 突合用)。**書込系は絶対に呼ばない**。
3. **トークン目標 +3000k**。
4. **知の広場は PROTECTED のまま実装完了まで**(Zulip型topic+Polis型投影 = 第8回裁定で採用確定)。解除 6 ゲート(G1〜G6)の判定材料を完成品まで作る(正本: `ihl-ver2/docs/planning/w2-checkpoint/知の広場-仮採用-MASTER-v1.md`)。HG-KN-01〜08 は帰宅後裁定 — 分母除外・停止報告。
5. ユーザーは外出中・/remote-control で軽い質問には答えられるが、**ブロッキングしない設計にする**こと。

## 2. C5 の進め方(必須手順)

1. **クラスタ分割表を最初に作る**: レジストリ(`01-requirements/registry.json`)から第1波 Tier A 149 件を抽出しカテゴリ単位に 6〜8 クラスタ化 → `docs/planning/c5/c5-cluster-table.md`。**この時点で開発計画 §4.3 の 3 点見積を改訂**(見積の生存期間が C5 開始までと明記されている — 義務)。
2. クラスタごとに: 設計契約(確定値)→ Workflow 実装(逐次ステージ+批評家2〜4本+修正ループ ≤2)→ 私(コーディネータ)が最終 GATE 再実測 → commit/push。C2〜C4 で確立したパターンを踏襲(下記 §4)。
3. 実装済み資産を必ず再利用: ScreenDef Renderer/ThemePack(画面はコードでなく screen-defs/*.json 追加)・truth 投影パターン・route-matrix(ver3_status 更新)。
4. 自動化可能 TC 80% 緑が wave 受け入れ規約(srs §8)。要件↔TC 対応表を `docs/planning/c5/tc-coverage.md` に。
5. 知の広場: 実装は保護 route 配下・「未実装/WIP」UI 表記禁止(V3-UIX-01)・G1〜G6 材料を `docs/planning/c5/plaza-gate-materials/` に完成品で。

## 3. C6 の到達上限(cutover 直前で停止)

- やってよい: 突合ハーネス(同一 GET → 新旧 JSON diff・許容差 whitelist)・57 route の readiness 表・staging デプロイ(承認済み)・E2E on staging。
- **絶対にやらない**: 本番 DNS/Pages/route 変更・ver3-live 停止・VPS 解約・本番書込系呼び出し・R2 本番 Truth への新 API からの書込有効化(書込系 route の片系切替は cutover の一部 = 人間ゲート)。
- 成果物: `docs/planning/c6/cutover-readiness.md`(route 別 green/red・残差分・人間が押すボタンの手順書)。

## 4. 確立済みの運転パターン(C2〜C4 で実証)

- Workflow: 実装ステージは model:'opus'・逐次(同一repo編集の衝突回避)・独立ディレクトリのみ並走可。批評家は effort:'high'+CRITIC_SCHEMA(verdict/findings)・「rubber-stamp 禁止・疑わしきは fail・実測エビデンス必須」。修正ループ最大2回 → 私が最終再実測。
- コミット: 担当ファイルのみ明示 add(git add -A 禁止)・「feat(c5): … — 自律実行(ultracode C5)・参照 REPORT-ver3-phase-c5-<date>」・事前 secrets grep。
- 完了時: REPORT-<phase> + status.md + memory 更新。

## 5. 環境の癖(今回の実測で判明・ハマり回避)

- Python は `C:/Users/sawad/AppData/Local/Programs/Python/Python312/python.exe` フルパス(`python` は PATH に無い)。コンソールは cp932 — `PYTHONIOENCODING=utf-8` を付ける。
- 権限分類器(bypassPermissions でも作動する敵対パターンガード): 金銭系 API 実行・DNS/ドメイン作成・自己権限変更は拒否される。ユーザー明示承認後もサブエージェント側で通る/通らないが揺れる — 金銭系は設計から外す(C5/C6 に金銭実行は無い)。
- sunabar: 日付は JST 基準(UTC で作ると 0〜9 時に当日不可視・過去日付400)。個人 API の依頼人名は remarks「振込 <全角名>」着地。
- Playwright: テスト名に日本語/矢印を入れると test-results の非ASCIIパスで filename lint が落ちる(apps/web の gitignore/lint 除外は対応済みだが、新規 E2E のテスト名は ASCII 推奨)。
- defuddle CLI 未導入(Web リサーチは WebFetch 代替可・導入すればなお良い)。
- wrangler は 4.20.0 固定(Node 20)。R2 実バケット `ihl-ver3-truth-dev` は dev 用に存置。
- `.dev.vars` は gitignore 済み。実鍵は D:\env\platform.env(値を表示・コミットしない)。

## 6. kickoff(新セッションに貼る1行)

```
ultracode +3000k。docs/planning/c5/HANDOFF-c5-c6-2026-07-11.md を読み、C5(クラスタ分割表+見積改訂→A帯149件消化)→C6(cutover直前まで)を進めて。
```
