---
id: RULING-2026-08-01-S5REG
title: ver3 ユーザー裁定記録 — 2026-08-01 S5裁定(採番2件=極性・却下理由 + 方針1件=参照回数を主指標にしない + 保留1件=代替不能性)
date: "2026-08-01"
status: active
---

# ver3 ユーザー裁定記録 — 2026-08-01 S5裁定(g72-s5reg)

> 入力(逐語原本): `D:\claude\00-hq\review-queue\R0731-0b2830-s5saiban-2026-08-01.json`(`user_ruling`欄。
> 回答=S5-1:o / S5-2:o / S5-3:h(保留) / S5-4:o、score=70、answered_at 2026-08-01T01:57:57.019Z)。
> 中間資料: 考える役 `D:\claude\00-hq\kits\lane-think\R0731-0b2830-REPORT-2026-07-31-g65-datavalue.md`
> (§5推奨・§11出口B/C)、設計艦 `D:\claude\00-hq\proposals\R0801-436936-DESIGN-2026-08-01-g67-refdesign.md`(§12出口D)。
> 発注書: `D:\claude\00-hq\kits\lane-implement\ORDER-2026-08-01-g72-s5reg.md`(発注者=72代目HQ hq-relay-20260801-1056)。
> 総数 770 → **772**(+2)。新規採番 **2件**(V3-AIP-110・V3-AIP-111)。既存patch **1件**(V3-AIP-45・ruling_note追記=S5-4方針)。
> req_status変更: 確定669→**671**(新規2件が確定で追加)。
> wave変更: 第1波363→**365**(+2)。
> S5-3(代替不能性フラグ)は**保留のまま**採番せず、本記録に保留理由とともに記録する(却下でも確定でもない)。
> 検算恒等式・件数突合は本記録 §7 参照(レジストリJSON実カウントを正とする)。

## 1. ユーザー回答原文(逐語・要約禁止)

### 1.1 カード「R0731-0b2830-s5saiban-2026-08-01」(score=70・2026-08-01T01:57:57.019Z)

| key | 回答 | コメント |
|---|---|---|
| S5-1(引用の極性=支持/反論) | `o` | (なし) |
| S5-2(却下理由の保存=機械可読) | `o` | 「とても、大事です。」 |
| S5-3(代替不能性フラグ) | `h`(保留) | 「微妙な設計人がYes No選ばないといけない？」 |
| S5-4(参照回数を主指標にしない) | `o` | (なし) |

**good欄逐語(反映の段取りに関する追加コメント。本回のS5判断そのものへの回答ではなく、次波の優先順位についての言及)**:

> 反映の段取り(依存関係つき)
> 波
> 内容
> 波1
> 要件登録(D-1受領時刻 / D-2欠測の明示 / D-3コホート完結性)+ 利用許諾(最優先)
> 波2
> 設計書。D-1〜D-4の実装設計と、index/payload の prefix 分離(★これだけは後からだと全件読み直し)
> 波3
> 実装 + マット関連(他社マットの一級市民化 / QR紐付け / 仮説の事前登録 / 工場IoTの正本はIHL)
>
> これらで決めたり話したことを元にすると提案変わってくるよね？

★このgood欄はD-1/D-2/D-3(背骨裁定・別記録`user-ruling-2026-08-01-backbone-d1d2d3.md`で処理済み)の反映段取りに関する言及であり、本回のS5-1〜S5-4の4判断とは無関係。本記録では処理対象としない(HQの反映段取り運用マターとして別途扱う)。

**bad欄**: (空)

## 2. 記録のみ(採番不要の運用指示。registryへは反映しない)

- S5-3(代替不能性フラグ)は`h`(保留)回答であり、採番せず**保留として本記録に残す**(§5-3参照)。

## 3. 処置マッピング(原文 → 処置)

| # | 対象 | 処置 | 結論 | 適用ルール |
|---|---|---|---|---|
| 1 | S5-1(引用の極性=支持/反論) → V3-AIP-110(新規) | 新規採番 | 承認(o) — 引用リンクに極性(支持/反論)の任意フィールドを追加。過去分は限定的機械導出のみ、残りは不明のまま | R3,R6 |
| 2 | S5-2(却下理由の保存=機械可読) → V3-AIP-111(新規) | 新規採番 | 承認(o・コメント「とても、大事です。」) — lineage-metaに状態/却下理由の任意フィールドを追加。V3-AIP-75(Evaluator)との関係を明記 | R3,R6 |
| 3 | S5-3(代替不能性フラグ) | 保留(却下ではない) | ユーザーコメント「微妙な設計人がYes No選ばないといけない？」を理由に保留。次アクション=考える役が第3案を再設計してから再提示 | R5(保留) |
| 4 | S5-4(参照回数を主指標にしない) → V3-AIP-45(既存patch) | 方針記録(ruling_note追記) | 承認(o) — 採番ではなく既存要件V3-AIP-45のruling_noteへ方針として追記。カウンタの実装・表示は変更しない | R6 |

## 4. 新規採番

1. **V3-AIP-110**(引用の極性=支持/反論): group=g09-AIプロセス・category=AI運用/開発プロセス・kind=機能要件・
   req_status=確定・wave=第1波(score_v2=36・tier_v2=A・ds=3。直接の類似要件V3-AIP-108/109に倣って記録役が
   付与した判断であり、ユーザー本人が個別に指定した値ではない — §9参照)。詳細statement・sourcesは
   registry.json本体を参照。
2. **V3-AIP-111**(却下理由の保存=機械可読): group=g09-AIプロセス・category=AI運用/開発プロセス・kind=機能要件・
   req_status=確定・wave=第1波(score_v2=36・tier_v2=A・ds=3。V3-AIP-110と同一の判断根拠 — §9参照)。
   V3-AIP-75(Evaluator=NG判断・却下理由を貴重な学習データとして保存)との関係を statement 内に1行明記した
   (発注書指示どおり・V3-AIP-75を実読してから記述)。詳細statement・sourcesはregistry.json本体を参照。

## 5. 各裁定の詳細(R6形式)

1. **V3-AIP-110 新規承認(引用の極性)**(R3,R6): 逐語は§1参照。回答=o。
   citation-link.schema.json(全46行)を実読し、link_kind 4値(cites/discussed_in/derived_from/responds_to)の
   いずれも支持/反論の極性を表さないことを自己確認した上でstatementに明記した。

2. **V3-AIP-111 新規承認(却下理由の保存)**(R3,R6): 逐語は§1参照。回答=o、コメント「とても、大事です。」。
   発注書の指示どおりV3-AIP-75を実読し(Evaluator=OK/NG評価ループ・「失敗(NG判断・却下理由)は成功データより
   貴重な学習データとして保存・活用する」)、本要件がその思想を系譜メタ(lineage-meta.schema.json・V3-FND-15)の
   却下理由フィールドとして具体化する関係にあることをstatementに1行明記した。

   ### 5-1. ★V3-AIP-75との関係(発注書の明示的指示への対応)

   V3-AIP-75は生成物(画像・字幕・タイトル等)へのOK/NG評価ループ(Evaluator)の要件であり、「失敗は成功
   データより貴重」という思想は既に確定している。V3-AIP-111はこの思想を、生成物の評価だけでなく
   **要件・設計判断そのものの却下理由**(系譜メタのstatus/rejection_reason)にも適用する要件であり、
   V3-AIP-75が定めた思想の別文脈(意思決定ログ)への展開にあたる。

3. **S5-3(代替不能性フラグ)は保留のまま記録**(R5・却下ではない):

   ### 5-2. ★保留理由(逐語のまま残す)

   ユーザーの問い(逐語)「微妙な設計人がYes No選ばないといけない？」をそのまま保存する。この問いは、
   代替不能性フラグを記録するには**記録時に人(ユーザーまたは記録者)がyes/noを1つ選ぶ運用**が最も安いと
   考える役が結論した点(R0731-0b2830 §10-4)に対する疑義であり、「その記入負荷を誰が・どこまで負うのか」
   が明確でないまま○/×を選ばせる設計になっていたことへの正当な指摘である。

   ### 5-3. ★保留理由の要約と次アクション

   - **保留理由**: 人の記入負荷を増やす運用への疑義。ユーザーは過去に記入作業を増やす案を繰り返し却下して
     おり(§8末尾参照)、「誰がYes/Noを選ぶか」が曖昧なまま採番すると同じ構造の負荷増になりうる。
   - **次アクション**: 「人がYes/Noを選ばない」経路(機械導出できる範囲だけ埋める等)の第3案を、考える役
     (lane-think)が再設計してから再提示する。AI毎回判定による自動判定案は既に課金増を理由に却下済み
     (R0731-0b2830 §2.2)であり、同じ案を蒸し返さないことをここに明記する。
   - **確定させていないこと**: S5-3を要件として**採番していない**。却下(×)としても**確定させていない**
     (§8参照)。

4. **V3-AIP-45 既存patch(ruling_note追記=S5-4方針)**(R6):

   回答=o。「参照回数単独でデータの取捨を自動判断しない(観測用・表示用に留める)」方針をV3-AIP-45の
   ruling_noteへ追記した(append-only。既存の2026-07-31付ruling_note=V3-AIP-45降格経緯はそのまま残し、
   末尾に本回の追記を連結)。考える役の判定(R0731-0b2830 §3.3)を要旨として引用し、カウンタ(S1〜S3)の
   実装・表示は変更しないことを明記した。**採番はしていない**(方針記録のみ)。

## 6. レジストリ反映指示(適用者=lane-implement 自身。本記録と同時に実施済み)

各追加/patchは**append のみ・既存値の削除禁止**の原則で、`01-requirements/registry.json` に直接適用した
(Pythonスクリプトによる機械適用・検算通過後に書き込み。スクリプトの一時保存先は艦のジョブ一時領域であり
リポジトリには残していない):

- 新規登録: **2件**(V3-AIP-110・V3-AIP-111。§4参照)。
- 触れた既存ユニークID **1件**(V3-AIP-45): `ruling_note`へS5-4方針(考える役§3.3判定の要旨)を追記
  (append-only。既存の`ruling_note`=2026-07-31付降格経緯はそのまま残し末尾に連結)。
- `req_status`変更: 確定669→**671**(新規2件による純増のみ。V3-AIP-45のreq_status変更は0件=`確定(修正)`のまま)。
- `wave`変更: 第1波363→**365**(新規2件。V3-AIP-45のwave変更は0件)。
- S5-3は要件として**登録していない**(§5-3参照)。
- `docs/planning/c8/progress.json`は**触っていない**(発注書「絶対に触るな」に厳密に従った。適用対象の
  JSON行は本記録と同じ内容をREPORT(`kits/lane-implement/R0801-4c2cb3-REPORT-2026-08-01-g72-s5reg.md`)へ貼る)。

## 7. 検算結果(実測値)

適用者=lane-implement(実測日 2026-08-01)。`registry.json` を Python(`json.load`)で実カウント。

**before(適用前)**: 総数770 / req_status: 確定669・確定(修正)93・棄却8 /
wave: 第1波363・第2波239・実験枠20・対象外148

**after(適用後・実測)**:
```
AFTER {'total': 772, 'req_status': {'確定': 671, '確定(修正)': 93, '棄却': 8}, 'wave': {'第1波': 365, '対象外': 148, '第2波': 239, '実験枠': 20}}
dup ids 0
entries without id []
V3-AIP-110 exists: True
V3-AIP-111 exists: True
```

- 671+93+8=772 ✓ / 365+239+20+148=772 ✓ / ID重複0 ✓ / id欠落0件 ✓
- 総数の純増分(+2)はV3-AIP-110・V3-AIP-111の新規登録のみに起因する(§4)。既存1件(V3-AIP-45)の
  統計上の変更は0件(ruling_note追記のみ、req_status/wave/score_v2は不変)。

## 8. 決められなかったこと / 正直な一覧

1. **V3-AIP-110/111のカテゴリ・グループ選択(AIP/g09-AIプロセス)は記録役の判断**。ユーザー逐語はカテゴリを
   指定していない。V3-AIP-108/109と同一の文脈(データ価値のAI判定)と判断してAIPカテゴリへ分類した。
2. **score_v2/tier_v2/ds/wave/scores(bv/ui/dep/diff)は記録役の代行判断**であり、ユーザー本人の指定ではない。
   round-23(V3-AIP-108/109)の先例(直接の類似要件の値に倣う)と同型の手法を踏襲した。HQ/lane-thinkによる
   確認・修正の余地を残す。
3. **S5-1の過去分機械導出(responds_to×却下済み裁定)が実際に何件埋まるかは未実測**。カード評価文が
   「課金ゼロの経路がある」と確認済みとしているのみで、実装時に件数を測る必要がある。
4. **S5-2の過去分抽出(自由記述からのマーカー抽出)が実際に何件埋まるかも未実測**。同様に実装時の確認が
   必要。
5. **S5-3の「人がYes/Noを選ばない」第3案の具体的な設計は本記録では行っていない**(考える役への差し戻し
   事項。本レーンの権限外)。
6. **S5-4の「では何が主指標か」への答えは未確定のまま**。ruling_noteにもその旨を明記した(誇張ゼロ)。
7. **`progress.json`の2行(V3-AIP-110/111、いずれもstatus: todo想定)は本レーンが直接書き換えていない**。
   発注書「絶対に触るな」に厳密に従い、該当JSON行はREPORT(報告書)に貼るのみ。適用はHQが行う。

## 9. 判断が要った箇所(実際に混ざった判断)

1. **V3-AIP-110/111をAIPプレフィックス(カテゴリ=AI運用/開発プロセス・グループ=g09-AIプロセス)に分類
   したこと**(§8-1)。発注書はプレフィックスを明示していない。
2. **score_v2=36/tier_v2=A/ds=3/wave=第1波/scores(bv=4,ui=2,dep=3,diff=3)を、直接の類似要件V3-AIP-108/109の
   値をそのまま踏襲する形で付与したこと**(§8-2)。round-23の先例に倣った代行判断。
3. **S5-4の方針をV3-AIP-45のruling_noteへ追記する対象として選んだこと**(発注書はV3-AIP-45/108/109の
   いずれか適切なものと指定。V3-AIP-45が「参照回数(reference_counter)を…記録する」の直接の出典要件で
   あるため最も適切と判断した)。
4. **S5-4のruling_note文言を、考える役R0731-0b2830 §3.3の判定を要旨として引用する形で構成したこと**
   (逐語全文ではなく要旨。全文はR0731-0b2830本体・カードのquoteフィールドに既にある)。
5. **good欄の反映段取りコメントを本記録の処理対象外と判断したこと**(§1参照)。S5-1〜S5-4の4判断とは
   無関係な別マター(D-1/D-2/D-3の反映段取り)であり、本発注書のスコープ外と判断した。
