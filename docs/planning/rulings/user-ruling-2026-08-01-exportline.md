---
id: RULING-2026-08-01-EXPORTLINE
title: ver3 ユーザー裁定記録 — 2026-08-01 exportline裁定(観測データexportの内部運用線引き・V3-SEC-60新規採番1件)
date: "2026-08-01"
status: active
---

# ver3 ユーザー裁定記録 — 2026-08-01 exportline裁定(g78-exportline)

> 入力(逐語原本): `D:\claude\00-hq\review-queue\R0801-149b70-exportline-2026-08-01.json`(`user_ruling`欄。
> 回答=EXP-1:o、score=75、answered_at 2026-08-01T03:57:12.544Z)。
> 中間資料: 批評ゲート艦報告書 `D:\claude\00-hq\kits\lane-research\R0801-149b70-REPORT-2026-08-01-g72-consentgate.md`
> (中1指摘=export線引き)、考える役の設計裁定案 `D:\claude\00-hq\kits\lane-think\R0801-439da4-REPORT-2026-08-01-g77-b1think.md` §2。
> 発注書: `D:\claude\00-hq\kits\lane-record\ORDER-2026-08-01-g78-exportline.md`(発注者=78代目HQ hq-relay-20260801-1542)。
> 総数 772 → **773**(+1)。新規採番 **1件**(V3-SEC-60)。既存エントリの改変 **0件**。
> req_status変更: 確定671→**672**(新規1件が確定で追加)。
> wave変更: 第2波239→**240**(+1)。
> 検算恒等式・件数突合は本記録 §7 参照(レジストリJSON実カウントを正とする)。

## 1. ユーザー回答原文(逐語・要約禁止)

### 1.1 カード「R0801-149b70-exportline-2026-08-01」(score=75・2026-08-01T03:57:12.544Z)

| key | 回答 | コメント |
|---|---|---|
| EXP-1(A案=exportを内部運用専用と明記し、将来の第三者提供の出口にはconsent_l3強制を要件として登録する) | `o` | (comments欄なし) |

**ifYes逐語(A案採用時に実施する内容)**:

> ○の場合: 要件1件登録(V3採番)+export仕様への内部用明記(記録艦1隻・一度きり)。

**good欄逐語**: 「よきかと」

**bad欄**: (空)

## 2. 記録のみ(採番不要の運用指示。registryへは反映しない)

該当なし(今回の判断は1件のみで、全て採番に直結する)。

## 3. 処置マッピング(原文 → 処置)

| # | 対象 | 処置 | 結論 | 適用ルール |
|---|---|---|---|---|
| 1 | EXP-1(export内部運用線引き) → V3-SEC-60(新規) | 新規採番 | 承認(o・score75・good「よきかと」) — `GET /observation/export` を内部運用専用と定義し、将来の第三者提供出口新設時にconsent_l3フィルタ強制を要件化 | ifYes逐語 |

**「export仕様への内部用明記」**(ifYes逐語の後半)は、b1think裁定案(§2冒頭「担当外」欄)に「2026-08-01実施済み=今回の範囲外」と明記されており、本記録・本レーンの対象外(コード明記自体は既に完了)。

## 4. 新規採番

1. **V3-SEC-60**(観測データexportの内部運用専用線引き+将来のconsent_l3強制): group=g05-ガバナンス法務・
   category=セキュリティ/法務・kind=機能要件・req_status=確定・wave=第2波(score_v2=21・tier_v2=B・ds=2。
   同カテゴリの直接の類似要件V3-SEC-59(不正検知強化・線引きの明文化のみで実装は次回以降)の値傾向に倣って
   記録役が付与した判断であり、ユーザー本人が個別に指定した値ではない — §9参照)。詳細statement・sourcesは
   registry.json本体を参照。

## 5. 裁定の詳細(R6形式)

1. **V3-SEC-60 新規承認**(EXP-1・ifYes): 逐語は§1参照。回答=o、score=75。

   ### 5-1. ★事実確認(記録レーンが着手前に実測したこと)

   - `GET /observation/export` の現在地は `apps/api/src/observation-routes.ts:976`(ルート定義)。批評ゲート
     報告書が指摘した当時の行番号`:963-970`から実測でズレていることを確認し、statementには現在行番号(:976)を
     採用した(KIT-TEMPLATE「行番号は必ず実物を確認」に対応)。
   - `isThirdPartyProvisionConsented`(consent_l3判定関数)は `observation-constants.ts:243` に実在し、
     `git grep` で本番コードからの呼び出しが0件であることを批評ゲート報告書が実測済み(=export側は現状
     consent_l3を参照していない。statementの「現時点の挙動は変更しない」の裏付け)。
   - commit `c7a7d7e` は `git log --oneline -1 c7a7d7e` で実在確認済み
     (`feat(ihl3): obs-captureにconsent_l3(L3=第三者提供・AI学習コーパス許諾)を追加`)。

   ### 5-2. ★採番プレフィックスの選定(b1think裁定案から変更なし)

   b1think裁定案 §2-4 は `V3-SEC` を推し、着手時の再測を指示していた。着手時に `"id": "V3-SEC-\d+"` の全件を
   再測した結果、最大番号は依然 `V3-SEC-59`(59件・変わらず)であり、並走艦による採番の割り込みは無かった。
   よって **`V3-SEC-60`** で確定した(b1think裁定案の想定どおり)。

   ### 5-3. ★statement・sourcesの推敲(b1think草案からの変更点)

   b1think裁定案 §2-4 の草案の骨(内部運用専用・将来出口にconsent_l3強制・現状挙動不変)は変更していない。
   推敲した点は以下のみ:
   - ルート行番号を草案には無かった具体値(`:976`)に差し替え、判定関数の場所(`observation-constants.ts:243`)も
     文中に明記した(検証可能性を上げるため)。
   - sourcesの2件目(consentgate報告書)の引用範囲を「:88-104」から実測の該当節「:75-104(中1全体)」に修正した
     (草案の行範囲がやや狭く、指摘の要旨(判定関数呼び出し0件の実測)が含まれる冒頭行を含めるべきと判断)。

## 6. レジストリ反映指示(適用者=記録レーン自身。本記録と同時に実施済み)

`01-requirements/registry.json` へ**append のみ・既存値の削除禁止**の原則で直接適用した(Edit ツールによる
直接編集・検算通過後に確定。一時スクリプトは作成していない):

- 新規登録: **1件**(V3-SEC-60。§4参照)。
- 触れた既存エントリ: **0件**(既存772件の値は1バイトも変更していない)。
- `req_status`変更: 確定671→**672**(新規1件による純増のみ)。
- `wave`変更: 第2波239→**240**(新規1件)。
- `docs/planning/c8/progress.json`は**触っていない**(本発注書の対象外。KIT-TEMPLATE「艦はprogress.jsonを
  直接編集しない」に従った)。

## 7. 検算結果(実測値)

適用者=記録レーン(実測日 2026-08-01)。`registry.json` を Python(`json.load`)で実カウント。

**before(適用前)**:
```
total 772
req_status Counter({'確定': 671, '確定(修正)': 93, '棄却': 8})
wave Counter({'第1波': 365, '第2波': 239, '対象外': 148, '実験枠': 20})
dup ids 0
missing id 0
max V3-SEC 59
```

**after(適用後・実測)**:
```
total 773
req_status {'確定': 672, '確定(修正)': 93, '棄却': 8}
wave {'第1波': 365, '対象外': 148, '第2波': 240, '実験枠': 20}
dup ids 0
missing id []
V3-SEC-60 exists: True
sum req_status 773
sum wave 773
```

- 672+93+8=773 ✓ / 365+240+20+148=773 ✓ / ID重複0 ✓ / id欠落0件 ✓
- 総数の純増分(+1)はV3-SEC-60の新規登録のみに起因する(§4)。既存772件の統計上の変更は0件。
- `git diff --numstat`: `64  0  01-requirements/registry.json`(64行挿入・**削除0行**)。削除行(`^-[^-]`)を
  正規表現で数えた結果も **0件**。append-onlyであることを機械的に確認した。

## 8. 決められなかったこと / 正直な一覧

1. **V3-SEC-60のカテゴリ・グループ選択(セキュリティ/法務・g05-ガバナンス法務)は記録役の判断**。ユーザー
   逐語・b1think裁定案のいずれもグループ/カテゴリを明示指定していない。同カテゴリの直接の類似要件
   V3-SEC-59(線引きの明文化のみで実装は次回以降、という同型の性質)に倣って分類した。
2. **score_v2/tier_v2/ds/wave/scores(bv/ui/dep/diff)は記録役の代行判断**であり、ユーザー本人の指定ではない。
   V3-SEC-59の値傾向(score_v2=24・tier_v2=B・ds=3・wave=第2波)に倣いつつ、本件はV3-SEC-59より実装可能性が
   さらに低い(実装スコープ自体が未着手)ためスコアをやや下げた(score_v2=21・ds=2)。この換算に厳密な
   公式は無い(`01-requirements/srs.md` を検索したがscore_v2の算出式の記載は見当たらなかった)。HQ/lane-think
   による確認・修正の余地を残す。
3. **`GET /observation/export` を将来「内部運用専用」の定義からさらに絞る(例: IPアドレス制限・監査ログ必須化
   等)べきかどうか**は本裁定の対象外。統ゲート報告書(中1)が提示した論点のうち「exportは内部用」の線引き
   のみを確定させ、それ以外の追加防御策は次回以降の検討に委ねる。
4. **将来、第三者提供の出口を実際に新設する際、consent_l3フィルタの実装作業自体**(工数・設計)は本裁定の
   対象外(本裁定は要件登録=線引きの明文化のみ)。

## 9. 判断が要った箇所(実際に混ざった判断)

1. **statement文中にルート行番号(`:976`)と判定関数の場所(`observation-constants.ts:243`)を明記したこと**
   (§5-3)。b1think草案には無かった具体化。検証可能性を優先したが、将来これらの行番号がずれる可能性は
   ある(その場合statementの記述と実物が乖離する)。
2. **V3-SEC-60のカテゴリ・グループ・score_v2/tier_v2/ds/waveの値をV3-SEC-59の値傾向に倣って決めたこと**
   (§8-1・§8-2)。他の候補(V3-AIP系・g09-AIプロセス)もあり得たが、「同意/第三者提供」という主題が
   セキュリティ/法務カテゴリの既存要件群(V3-SEC-47・V3-SEC-59)に近いと判断した。
3. **sourcesの2件目(consentgate報告書)の引用範囲をb1think草案の「:88-104」から「:75-104」へ広げたこと**
   (§5-3)。判定関数の呼び出し0件という実測(中1冒頭)を含めないと、statementの「現時点の挙動は変更しない」
   の根拠が弱く見えると判断した。
4. **「export仕様への内部用明記」(ifYes逐語の後半)を本記録の処置対象から除外したこと**(§3末尾)。
   b1think裁定案が「2026-08-01実施済み・今回の範囲外」と明記しており、発注書(ORDER-2026-08-01-g78-exportline.md
   T1冒頭)も同じ前提で範囲を「登録+裁定記録のみ」に絞っていたため、それに従った。自分では実施状況を
   再検証していない(検証すればより正確だが、発注書のスコープ外の作業になるため見送った)。
