---
id: HANDOFF-c7-2026-07-11
title: C7 引き継ぎ正本(実画面検証→打鍵チェック準備+cutover 実行)
date: "2026-07-11"
status: active
---

# HANDOFF: C7(実画面検証 最優先 → cutover 実行 → 余力枠)

> 新セッションはまず README → AGENTS.md → `docs/planning/status.md` → 本書の順で読むこと。
> 自律ラン既定契約(グローバル CLAUDE.md)適用。第13回裁定(`docs/planning/rulings/user-ruling-2026-07-11-round-13.md`)が本書の裁定根拠。

## 0. 現在地(2026-07-11 夜・全 push 済み)

- **C0〜C6 完了**(C6 は cutover 直前で停止)。lint 20 GATE・npm test 851+ passed・pytest 11p1s・実ブラウザ E2E 3/3 green。
- レジストリ 733 件(第13回で+8)・srs v1.8・保留 0。
- staging 実機: `https://ihl-api-ver3-staging.it-hercules-laboratory.workers.dev`(dev R2・cron 無効・dev secret のみ)。
- 突合実測: `docs/planning/c6/cutover-readiness.md`(66 route 表+人間ボタン 5 手順+P0 所見)。

## 1. 第13回裁定の要点(本ランの権限根拠)

1. **最優先 = 実画面検証→最終打鍵チェック準備**(V3-AIP-101・本人逐語「まずシステムが完璧にできることが最優先」「安心できる状態に本当になってから、私に最終画面打鍵チェックさせてください」)。
2. **cutover 技術手順(readiness §4 の 1〜4)は AI が実行してよい**。各不可逆ボタンの直前に一言報告・ユーザー在席時実施を推奨。**VPS 解約のみ人間**。DNS 操作が権限分類器に拒否されたら手順書ベースの伴走へ切替。
3. P0(旧 API 未認証開放 12 route)= 旧側は触らず **cutover 前倒しで自然解消**。
4. 知の広場 = **本採用 Go 済み**。PROTECTED 解除(一般公開)は cutover 後に別途裁定。
5. 余力枠(優先一段下・本人「できたらうれしい」): 動画システム準備(V3-VID-33/34)・家/研究所要件整理・技術記事/SNS パック(V3-AIP-102/103)。

## 2. C7 タスク(順序どおり)

### T1: 全画面 実ブラウザ検証(最優先・V3-AIP-101)

- 対象: `screen-defs/*.json` 全画面(navigation.json 登録 30 前後)。ローカル(wrangler dev + next dev)で Playwright 実機走査。
- 各画面: 描画・主要操作(フォーム送信・遷移・データ束縛)・エラーなしを実測し、**スクリーンショット付きレポート** `docs/planning/c7/screen-verification-report.md` に画面別 PASS/FAIL 表で。
- FAIL は真因修正(C6 の form→confirm バグのような結合欠陥を想定)。**モック・未配線を「動く」と書かない**。
- 完了条件: 全画面 PASS + レポート完成 → ユーザーへ「最終打鍵チェック依頼」を出せる状態。
- C5 K8 の生成器/E2E 資産を再利用: `gen-screendef-snapshots.mjs`(37TC)・`apps/web/e2e/*`・deferred-e2e 3 本(ugc-translate/market/spec-thread)の実走 green 化もここで。

### T2: cutover 実行(readiness §4 の 1〜4・ユーザー在席時)

1. `[env.production]` 起票(本番 R2 バインド)→ deploy(直前一言報告)
2. 突合リハーサル(`scripts/reconcile-harness.mjs` に `--params` で実 ID)— 値レベル一致まで
3. planned 残 route(onboarding 2・gmo webhook 系・market transfer/match)を実装 or 廃止処置してから片系切替(直前一言報告)
4. ver3-live 停止(トラフィック 0 確認後・直前一言報告)。**VPS 解約はユーザー**

### T3: 余力枠(T1/T2 完了後のみ)

- V3-VID-33/34: 手本台本(V3-VID-31/32 規約準拠)+動画 Workflow 雛形
- 家/研究所要件整理: `D:\claude\整理予定\アイディア\家、研究所、測定　資料`(7 ファイル)→ 平野さん向け要望書(欲望強度順)
- V3-AIP-102/103: 記事パック+SNS 配信設計(公開ボタンは人間)
- V3-OBS-72(第1波): 研究室環境コンテキストのスキーマ+紐付け(K7 placement 基盤の拡張)

## 3. 環境の癖(C5/C6 実測の追記込み)

- Python フルパス `C:/Users/sawad/AppData/Local/Programs/Python/Python312/python.exe`+`PYTHONIOENCODING=utf-8`。Workflow args は直書き必須。
- サブエージェントのシェル事故で repo 直下に 0 バイト迷子ファイルが頻発 — 各波の commit 前に削除。生 NUL バイトをコードに書くと git がバイナリ扱い(テキストエスケープで書く)。
- wrangler: `CLOUDFLARE_API_TOKEN`/`CLOUDFLARE_ACCOUNT_ID` は D:\env\platform.env の `CF_API_TOKEN`/`CF_ACCOUNT_ID` から同一 PowerShell コール内で設定(コール間で消える)。secret put は改行なしファイルから `cmd /c "... < file"`(パイプは CR 混入)。
- **トップレベル [triggers] は env に継承される** — 新 env には `[env.X.triggers] crons=[]` を必ず明示(C6 事故の再発防止)。
- 実鍵は投入しない(staging は dev 生成値)。本番 secret 投入は cutover T2 の一部として直前報告。

## 4. kickoff(新セッションに貼る 1 行)

```
ultracode +3000k。docs/planning/c7/HANDOFF-c7-2026-07-11.md を読み、T1(全画面実ブラウザ検証→打鍵チェック準備)を最優先で、T2(cutover・私在席時に不可逆ボタン)、余力で T3 を進めて。
```

推奨設定: モデル=Fable 5(計画/レビュー)+ opus 実装サブエージェント(C5/C6 実証済み構成)・permission mode=bypassPermissions(設定済み)・トークン目標 +3000k(T1+T2 中心なら十分。T3 まで全部なら +4500k)。cutover ボタンを押す T2 はユーザー在席時間帯に。
