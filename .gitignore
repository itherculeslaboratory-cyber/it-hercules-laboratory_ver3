# dependencies / build artifacts
node_modules/
dist/
.next/
*.tsbuildinfo
.wrangler/
__pycache__/
.venv/
coverage/

# AI harness local state (session settings / temp worktrees) — never committed
.claude/

# secrets — real values never committed (.env.example is the only tracked template)
.env
.env.*
!.env.example
