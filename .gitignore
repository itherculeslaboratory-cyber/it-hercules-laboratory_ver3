# dependencies / build artifacts
node_modules/
dist/
.next/
*.tsbuildinfo
.wrangler/
__pycache__/
.venv/
coverage/

# AI harness local state (session settings / temp worktrees) — never committed
.claude/

# Playwright E2E run artifacts (reports/traces) — the committed evidence lives
# in docs/planning/c2/ (screenshots + e2e-evidence.md), not these.
test-results/
playwright-report/

# secrets — real values never committed (.env.example is the only tracked template)
.env
.env.*
!.env.example
!.env.platform.example
!.env.local.example
.dev.vars
