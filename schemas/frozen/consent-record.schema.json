{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.it-hercules.uk/frozen/consent-record.schema.json",
  "title": "Legal Consent Record (CL-05)",
  "description": "利用規約への技術的同意イベント。INSERT ONLY・複数同意=別ファイル（法的証跡なので形式変更不可 — 要件定義書 CL-05 / V3-SEC-20 / NFR-05）。ver2 apps/api/main.py の legal_agree が書き出す legal_agree_v1 レコードそのもの。最終法務文言は人間ゲート HUMAN-02-LEGAL 待ちのため is_draft_terms=true。",
  "x_ihl_cl": "CL-05",
  "x_ihl_status": "frozen",
  "x_ihl_layer": "truth",
  "x_ihl_source": "apps/api/main.py legal_agree (legal_agree_v1) + tests/unit/test_terms.py@4a56cf6",
  "type": "object",
  "required": ["schema", "agree_id", "actor_id", "terms_version", "is_draft_terms", "legal_gate"],
  "properties": {
    "schema": {
      "const": "legal_agree_v1"
    },
    "agree_id": {
      "type": "string",
      "pattern": "^agree_[0-9a-f]{12}$",
      "description": "os.urandom(6).hex() 由来。ファイル名 = <agree_id>.json（別ファイル方式）。"
    },
    "actor_id": { "type": "string" },
    "terms_version": {
      "type": "string",
      "description": "同意した規約バージョン（ver2 現行 = 'draft-2026-06'）。"
    },
    "is_draft_terms": {
      "type": "boolean",
      "description": "草案版への同意か。最終法務確定（HUMAN-02-LEGAL）前は true。"
    },
    "legal_gate": {
      "type": "string",
      "description": "この同意をブロックしている人間ゲート識別子（ver2 現行 = 'HUMAN-02-LEGAL'）。"
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "ver2 の永続レコードには created_at が書かれていない（apps/api/main.py の record dict に含まれない）。ver3 で付与するかは C1 実機照合で確定。"
    }
  },
  "additionalProperties": false
}
