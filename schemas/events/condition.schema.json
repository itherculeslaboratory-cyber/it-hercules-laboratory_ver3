{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.it-hercules.uk/events/condition.schema.json",
  "title": "Paper condition P entry (PPR-02)",
  "description": "論文の条件P(P⇒Qの前提)を構成する単一観点キーの閾値仕様(単一正本)。マッチアルゴリズム(paper-match.ts matchConditions/autoFillDescriptor)とcontent.schema.json(paper conditions)の両方がこのファイルだけを参照する(複製しない)。観点キー自体はcontent.schema.json側でオブジェクトのプロパティ名として持つ(このスキーマは1エントリの値側の形)。",
  "x_ihl_req": "V3-PPR-02",
  "x_ihl_source": "docs/planning/c5/design-c5.md (K5) §2.3",
  "type": "object",
  "required": ["required"],
  "properties": {
    "min": { "type": "number" },
    "max": { "type": "number" },
    "eq": { "type": "number" },
    "required": { "type": "boolean" },
    "unit": { "type": "string" }
  },
  "additionalProperties": false
}
