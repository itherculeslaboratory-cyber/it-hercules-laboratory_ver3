{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.it-hercules.uk/events/mkt-listing-photo.schema.json",
  "title": "Market Listing Photo data (ihl.mkt.listing_photo.v1)",
  "description": "出品写真イベント ihl.mkt.listing_photo.v1 の data 部（c8 UI磨き第2弾#2・受領10「画像がない」）。Truth キー truth/ihl.mkt.listing_photo.v1/<listing_id>-<photo_ulid>.json（listing_id 前方一致で R2 prefix list 可能・obs-photo.schema.json と同型）。バイナリは R2 media/photo/<photo_id> に put-if-absent（envelope は JSON のみ）。既存 mkt-listing 型はリネーム・破壊変更せず、写真は別イベント型として追記する（AGENTS.md 禁止事項「schemas/frozen の破壊変更」の精神を非 frozen 型にも適用）。",
  "x_ihl_req": "V3-MKT-23",
  "x_ihl_source": "docs/planning/rulings/round-16-answers-raw.md 受領10 ②",
  "type": "object",
  "required": ["photo_id", "listing_id", "actor_id", "media_key", "content_type", "size_bytes", "sha256"],
  "properties": {
    "photo_id": {
      "type": "string",
      "minLength": 1,
      "description": "写真の一意キー（<photo_ulid>）。"
    },
    "listing_id": {
      "type": "string",
      "minLength": 1,
      "description": "所属する出品の listing_id。id 規約 <listing_id>-<photo_ulid> の前方一致キー。"
    },
    "actor_id": {
      "type": "string",
      "minLength": 1,
      "description": "登録者の actor_id（出品者本人のみ・V3-AUT-17）。"
    },
    "media_key": {
      "type": "string",
      "pattern": "^media/photo/[^/]+$",
      "description": "R2 バイナリキー。'media/photo/<photo_id>' 固定（obs-photo と同一バケット配置・種別混在は photo_id の一意性で衝突しない）。"
    },
    "content_type": {
      "type": "string",
      "minLength": 1,
      "description": "MIME タイプ（例: image/jpeg・image/png）。"
    },
    "size_bytes": {
      "type": "integer",
      "minimum": 0,
      "description": "バイナリのバイト数。"
    },
    "sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "バイナリの SHA-256（16 進小文字 64 桁）。"
    }
  },
  "additionalProperties": false
}
