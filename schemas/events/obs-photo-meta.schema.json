{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.it-hercules.uk/events/obs-photo-meta.schema.json",
  "title": "Observation Photo QC Meta data (ihl.obs.photo_meta.v1)",
  "description": "写真QCメタイベント ihl.obs.photo_meta.v1 の data 部（OBS-58 QCフィルタ + OBS-12 view/file_kind/booth観測メタ）。g93裁定(00-hq\\kits\\lane-think\\R0802-37b8e4-REPORT-2026-08-02-g93-truththink.md §2-5/T3)による是正 — 実データ5/5件から必須5フィールドを確定。view/file_kind/booth_id/booth_typeはクライアント由来の任意観測メタで、実データには出現しないがコードは出力しうるため optional で含める(observation-routes.ts:616-619)。",
  "x_ihl_req": "V3-OBS-58",
  "x_ihl_source": "00-hq/kits/lane-think/R0802-37b8e4-REPORT-2026-08-02-g93-truththink.md §2-5",
  "type": "object",
  "required": ["photo_id", "capture_id", "qc_flag", "qc_score", "qc_reasons"],
  "properties": {
    "photo_id": {
      "type": "string",
      "minLength": 1,
      "description": "写真の一意キー。"
    },
    "capture_id": {
      "type": "string",
      "minLength": 1,
      "description": "所属する capture の capture_id。"
    },
    "qc_flag": {
      "type": "string",
      "minLength": 1,
      "description": "QC判定フラグ。観測値が 'reject' 1種のみで値域を確定できないため enum で絞らない(g93裁定§2-5)。"
    },
    "qc_score": {
      "type": "number",
      "description": "QCスコア。実データは整数値だが integer に絞る根拠が無い(g93裁定§2-5)。"
    },
    "qc_reasons": {
      "type": "array",
      "items": { "type": "string" },
      "description": "QC判定理由の配列。"
    },
    "view": {
      "type": "string",
      "description": "クライアント指定のcapture view(optional・observation-routes.ts:616)。"
    },
    "file_kind": {
      "type": "string",
      "description": "クライアント指定のファイル種別(optional・observation-routes.ts:617)。"
    },
    "booth_id": {
      "type": "string",
      "description": "クライアント指定のbooth識別子(optional・observation-routes.ts:618)。"
    },
    "booth_type": {
      "type": "string",
      "description": "クライアント指定のbooth種別(optional・observation-routes.ts:619)。"
    }
  },
  "additionalProperties": false
}
