{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.it-hercules.uk/events/mkt-listing.schema.json",
  "title": "Market Listing data (ihl.mkt.listing.v1)",
  "description": "マーケット出品イベント ihl.mkt.listing.v1 の data 部（V3-MKT-01・出品/閲覧まで）。Truth キー truth/ihl.mkt.listing.v1/<listing_id>.json に append。一覧/詳細は投影で都度再計算（不変条項①）。取引遷移（match/transition）・決済連動は C4 対象外（後波）。",
  "x_ihl_req": "V3-MKT-01",
  "x_ihl_source": "docs/planning/c4/design-c4.md §3",
  "type": "object",
  "required": ["listing_id", "actor_id", "title"],
  "properties": {
    "listing_id": {
      "type": "string",
      "minLength": 1,
      "description": "出品の一意キー（<listing_ulid>）。"
    },
    "actor_id": {
      "type": "string",
      "minLength": 1,
      "description": "出品者の actor_id（CL-03 導出・本人スコープ V3-AUT-17）。"
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "description": "出品タイトル。"
    },
    "description": {
      "type": "string",
      "description": "出品説明（任意）。"
    },
    "price": {
      "type": "integer",
      "minimum": 0,
      "description": "希望価格（円・任意）。決済連動は C4 対象外。"
    },
    "lang": {
      "type": "string",
      "description": "UGC 原文の作者言語タグ（BCP-47・actor locale 由来）。翻訳はしない＝I18-06。"
    },
    "accept_mode": {
      "type": "string",
      "enum": ["instant", "consent"],
      "description": "定価出品（list_fixed）の成立方式（round-16 OQ-MKT-02）。instant=即決（既定・省略時は instant 扱い・申込確定=即成立）／consent=承諾制（オプトイン・買い手の offer/love_letter を出品者が 24h 以内に承諾して初めて成立）。"
    },
    "reservation_sire_id": {
      "type": "string",
      "minLength": 1,
      "description": "V3-IND-35 割り出し予約: 対象の父個体 individual_id（♂・任意）。この listing を「予約 listing」化する（reservation_sire_id/dam_id のいずれかが有れば予約対象）。"
    },
    "reservation_dam_id": {
      "type": "string",
      "minLength": 1,
      "description": "V3-IND-35 割り出し予約: 対象の母個体 individual_id（♀・任意）。"
    },
    "reservation_min_apply_count": {
      "type": "integer",
      "minimum": 1,
      "description": "V3-IND-35: 出品側が事前設定する応募単位の最小匹数（この匹数未満の予約は自動マッチング対象外・任意）。"
    },
    "reservation_max_apply_count": {
      "type": "integer",
      "minimum": 1,
      "description": "V3-IND-35: 出品側が事前設定する応募単位の最大匹数（この匹数超の予約は自動マッチング対象外・任意）。"
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "出品時刻。"
    },
    "schema_version": {
      "type": "integer",
      "description": "data スキーマ版。"
    }
  },
  "additionalProperties": false
}
