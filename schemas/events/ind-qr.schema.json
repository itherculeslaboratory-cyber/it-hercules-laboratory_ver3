{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.it-hercules.uk/events/ind-qr.schema.json",
  "title": "Individual QR data (ihl.ind.qr.v1)",
  "description": "個体 QR 発行イベント ihl.ind.qr.v1 の data 部。Truth キー truth/ihl.ind.qr.v1/<token>.json。token は frozen qr-token（CL-10）と同形式（URL-safe base64・20〜200 字）でトークン形式は無変更。現物ラベル用途のため expires_at は任意（env QR の frozen スキーマは変更しない）。",
  "x_ihl_req": "V3-OBS-20 / FR-MVP-05 / V3-IND-01",
  "x_ihl_source": "docs/planning/c2/design-c2.md §3.1 / schemas/frozen/qr-token.schema.json (CL-10)",
  "type": "object",
  "required": ["token", "individual_id", "actor_id", "created_at"],
  "properties": {
    "token": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_-]+$",
      "minLength": 20,
      "maxLength": 200,
      "description": "URL-safe base64 トークン（frozen qr-token と同形式・CL-10 無変更）。deep link の起点。"
    },
    "individual_id": {
      "type": "string",
      "minLength": 1,
      "description": "紐づく個体 ID（観測再開の対象）。"
    },
    "actor_id": {
      "type": "string",
      "minLength": 1,
      "description": "発行者の actor_id。"
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "発行時刻（RFC3339）。"
    },
    "expires_at": {
      "type": "string",
      "format": "date-time",
      "description": "失効時刻（任意）。現物ラベル用途では無期限（未設定）を許容。"
    }
  },
  "additionalProperties": false
}
