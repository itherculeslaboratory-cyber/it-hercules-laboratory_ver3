{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.it-hercules.uk/events/fnd-restore-applied.schema.json",
  "title": "Foundation restore-applied event (ihl.fnd.restore_applied.v1)",
  "description": "復元ポイント適用の append-only 監査イベント（V3-FND-08）。Truth キー truth/ihl.fnd.restore_applied.v1/<actor_id>-<ulid>.json。★append-only の Truth では『巻き戻し』ができないため、復元は『過去内容を新しいULIDで現在時刻の新イベントとして再投稿する』操作として実装する（design19 T1-4 案A・design35からの訂正）。本イベントは復元操作そのものの事実（いつ・どの復元ポイントを・何件適用したか）だけを記録し、再投稿された各イベント自体は別途通常のイベントとして append される。",
  "x_ihl_req": "V3-FND-08",
  "x_ihl_source": "kits/lane-think/R0731-08b452-REPORT-2026-07-31-g64-design19.md §T1-4",
  "type": "object",
  "required": ["actor_id", "restore_point_id", "applied_event_count", "at"],
  "properties": {
    "actor_id": {
      "type": "string",
      "minLength": 1,
      "description": "復元を実行した actor_id（セッション principal 強制・V3-AUT-17）。"
    },
    "restore_point_id": {
      "type": "string",
      "minLength": 1,
      "description": "適用した復元ポイントの一意キー。"
    },
    "applied_event_count": {
      "type": "integer",
      "minimum": 0,
      "description": "再投稿されたイベント件数。"
    },
    "at": {
      "type": "string",
      "format": "date-time",
      "description": "復元適用時刻（RFC3339）。"
    },
    "schema_version": {
      "type": "string",
      "minLength": 1,
      "description": "data スキーマ版（任意）。"
    }
  },
  "additionalProperties": false
}
