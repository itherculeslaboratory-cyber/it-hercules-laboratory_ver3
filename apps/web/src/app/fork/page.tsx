// 創る(FORK)ゾーンのルート /fork。承認予想図の逐語採用 + 実データ配線。
// 配置(IA v2・R115): トップナビには置かず、ホームのカード + マイページから開く入口。
import ForkZone from "./fork-zone";

export const metadata = {
  title: "創る | IHL",
};

export default function ForkPage() {
  return <ForkZone />;
}
